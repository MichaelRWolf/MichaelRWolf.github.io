#! /bin/bash
set -euo pipefail

cd ~/repos/MichaelRWolf.github.io

chruby-exec 3.1.3 -- gem install bundler
chruby-exec 3.1.3 -- bundle install
rm -rf _site

 # May add: --livereload --drafts
chruby-exec 3.1.3 -- bundle exec jekyll serve "$@" --livereload &

open http://localhost:4000
